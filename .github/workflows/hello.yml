name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_word_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
          ls -l 